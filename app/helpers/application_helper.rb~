module ApplicationHelper

   #return row counts
   def RowCounts
                 
     @count = @bigsalads.length > 4 ? @bigsalads.length / 4 : 1
     
     if(@bigsalads.length % 4 > 0)
         @count += 1
     end
      
   end

end
